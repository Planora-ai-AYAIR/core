name: CI Frontend

on:
  pull_request:
    paths:
      - 'apps/frontend/**'
  push:
    paths:
      - 'apps/frontend/**'

jobs:
  frontend-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Frontend CI placeholder"
