name: CI Backend

on:
  pull_request:
    paths:
      - 'apps/api-dotnet/**'
  push:
    paths:
      - 'apps/api-dotnet/**'

permissions:
  contents: read

jobs:
  backend-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo ".NET API CI placeholder"
